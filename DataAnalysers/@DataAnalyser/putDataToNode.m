function  putDataAtNode(obj, dataObjS, node, uit, hObj)



% Add to node in tree
out = addToDataAnalysisFolder(obj, getValue(node), dataObjS, uit);

% collapseAndUnLoadTree(obj, get(hObj, 'Parent'));
% EOF
